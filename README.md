# 📖 Legado Web Reader

> 兼容 [阅读 (Legado)](https://github.com/gedoor/legado) 书源规范的 Web 端小说阅读器

Legado（阅读3.0）是 Android 平台上最受欢迎的开源阅读器之一（44k+ stars），其核心优势在于用户可自定义书源规则——通过 JSON 描述搜索、详情、目录、正文的提取规则，从任意网站抓取小说内容。

**Legado Web Reader** 将这一能力带到了浏览器端。轻量级前后端分离架构，支持导入 Legado 书源 JSON 文件，在桌面和移动端浏览器上即可享受与 Legado App 相同的书源生态。

---

## ✨ 核心特性

- **🔧 多规则引擎** — 兼容 Legado 书源的 CSS Selector、XPath、JSONPath、正则替换及 JSOUP Default 语法
- **🔍 多源搜索** — 输入关键词即可同时搜索所有已启用书源，快速找到你想读的书
- **📚 完整阅读流程** — 搜索书籍 → 查看详情 → 浏览目录 → 阅读正文，一站式体验
- **📥 本地书源导入** — 支持导入 `.json` 格式的 Legado 书源文件，即导即用
- **📱 响应式设计** — 适配桌面和移动端浏览器，随时随地阅读
- **🎨 阅读器可调** — 字体大小、行间距、页面宽度自由调节；支持亮色/暗色/护眼主题
- **⌨️ 键盘快捷键** — 方向键翻页，桌面端体验丝滑
- **💾 本地书架** — 收藏书籍、记忆阅读进度，下次打开继续阅读

---

## 🛠️ 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端** | Vue 3 + Vite + Pinia + TailwindCSS | 现代 SPA，响应式 UI |
| **后端** | Node.js + Express 5 | REST API，代理网络请求解决 CORS |
| **规则引擎** | cheerio / xpath / jsonpath-plus | 多引擎解析 HTML/JSON 响应 |
| **数据存储** | SQLite (better-sqlite3) | 书源持久化，轻量可靠 |
| **书架 & 进度** | localStorage | 前端本地存储，零配置 |

---

## 🚀 本地运行

### 前提条件

- [Node.js](https://nodejs.org/) >= 18
- npm 或其他包管理器

### 1. 克隆仓库

```bash
git clone https://github.com/noahal1en/legado-web-reader.git
cd legado-web-reader
```

### 2. 启动后端

```bash
cd backend
npm install
npm run dev
```

后端默认运行在 `http://localhost:3001`，提供 REST API 和网络代理。

### 3. 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端默认运行在 `http://localhost:5173`，打开浏览器访问即可。

### 4. 开始使用

1. 进入「书源管理」页面，导入你的 Legado 书源 JSON 文件
2. 在搜索页输入书名或作者，多源搜索
3. 选择搜索结果，查看详情和目录
4. 开始阅读！

---

## 📁 项目结构

```
legado-web-reader/
├── frontend/          # Vue 3 前端
│   ├── src/
│   │   ├── api/       # API 调用层
│   │   ├── components/# UI 组件
│   │   ├── views/     # 页面视图
│   │   ├── stores/    # Pinia 状态管理
│   │   └── utils/     # 工具函数
│   └── package.json
├── backend/           # Node.js 后端
│   ├── src/
│   │   ├── routes/    # API 路由
│   │   ├── engine/    # 规则引擎 (CSS/XPath/JSONPath/Regex)
│   │   ├── services/  # 业务逻辑
│   │   └── utils/     # HTTP 客户端, URL 处理
│   └── package.json
├── docs/design/       # 设计文档 (DD)
└── README.md
```

---

## ⚠️ 已知限制

- **不支持 JS 规则** — Legado 的 `@js:` 和 `<js>...</js>` 规则需要完整 JS 沙箱，计划在 v2 实现
- **不支持 WebView 模式** — 不支持 `sourceRegex` 资源嗅探
- **不支持登录认证** — 不处理 `loginUrl` / `loginUi`
- **仅支持文本源** — 不支持音频源（type 1）和图片/漫画源（type 2）
- **单用户设计** — 适合自部署场景，不提供多用户和权限管理

---

## 🤖 关于本项目

这个项目是一次有趣的实验——**它完全由 AI Agent 自主开发完成**，没有一行人类手写代码。

整个开发流程由 [OpenClaw](https://github.com/nicepkg/openclaw) 平台上的 AI Agent 协作完成：

| 角色 | Agent | 职责 |
|------|-------|------|
| 🎯 **调度** | Noah (主 Agent) | 项目规划、任务拆分、质量把关 |
| 📐 **设计 + 前端** | Claude | 撰写 DesignDoc、设计 API 契约、实现 Vue 3 前端 |
| ⚙️ **后端** | Codex | 实现 Node.js 后端、规则引擎、API 路由 |

采用 **DD (DesignDoc) 驱动开发工作流**：先由 Claude 撰写详尽的设计文档（定义架构、API 契约、规则引擎设计），经审核后 Claude 和 Codex 分别并行实现前端和后端，最后联调验收。

> _从设计文档到可运行的阅读器，全程自动，人类只负责说「开始」和「看起来不错」。_

---

## 📄 License

MIT

---

_Made with 🤖 by AI Agents on [OpenClaw](https://github.com/nicepkg/openclaw)_
